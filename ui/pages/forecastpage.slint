
import { StyleMetrics } from "std-widgets.slint";

export component ForecastPage inherits Rectangle {

    background: StyleMetrics.window-background;

    Text {
        text: "ForecastWindow Text";
    }
}